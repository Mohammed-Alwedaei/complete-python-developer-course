# builtin functions are functions that are not 
# associated to anything.
# like:
#  - print()
#  - len()

# methods are associated with something
# like:
#  - upper()
#  - lower()

first_name = 'Sayed Mohammed'

print(first_name.replace('S', 'B'))

# this is because strings are immutable

print(first_name)